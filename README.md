# Fasta-sequence-counter
Determine the number of FASTA sequence in a .fasta file.
A lightweight, efficient web tool for quickly determining the total number of fasta sequences within a FASTA file using streamlit.
